{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo 'No frontend build needed'",
        "build": "echo 'No frontend build needed'"
}
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
